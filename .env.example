# Copy to .env  ->  cp .env.example .env
DATABASE_URL="file:./dev.db"

# Optional: comma-separated emails auto-promoted to ADMIN + APPROVED on signup.
# (The first account to sign up always becomes an admin regardless.)
ADMIN_EMAILS=""
